# chase me
An experiment of a game built in Rust from scratch.

The project uses SDL2 for rendering.

## Set up
Requirements:
- Install [Rust](https://www.rust-lang.org/tools/install)

To build the project, use the Cargo commands:

- `cargo build`
- `cargo run`
